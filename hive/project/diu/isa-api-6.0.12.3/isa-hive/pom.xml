<?xml version="1.0" encoding="UTF-8"?><!--
   Apache Maven 2 POM generated by Apache Ivy
   http://ant.apache.org/ivy/
   Apache Ivy version: 2.4.0 20141213170938
--><project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <groupId>owner.isa.hive</groupId>
  <artifactId>isa-hive</artifactId>
  <version>1.0.0</version>

  <properties>
    <maven.compiler.source>1.7</maven.compiler.source>
    <maven.compiler.target>1.7</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
  </properties>

  <repositories>
    <repository>
      <id>local-maven-repo</id>
      <url>file:///${basedir}/../maven</url>
    </repository>
  </repositories>

  <dependencies>

    <dependency>
      <groupId>dsif</groupId>
      <artifactId>isa-spi</artifactId>
      <version>6.0.12.3</version>
      <scope>compile</scope>
    </dependency>

    <dependency>
      <groupId>mil.army.nvl.isa</groupId>
      <artifactId>isa-api</artifactId>
      <version>6.0.12.3</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.aeonbits.owner</groupId>
      <artifactId>owner</artifactId>
      <version>1.0.12</version>
    </dependency>

    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>2.22.0</version>
    </dependency>

    <!-- OPTIONAL EXTENSIONS-->

    <dependency>
      <groupId>mil.army.nvl.isa</groupId>
      <artifactId>api-extended</artifactId>
      <version>6.0.10.2</version>
      <scope>compile</scope>
    </dependency>

    <dependency>
      <groupId>mil.army.nvl.isa</groupId>
      <artifactId>api-cbrn</artifactId>
      <version>6.0.6.2</version>
      <scope>compile</scope>
    </dependency>

    <dependency>
      <groupId>mil.army.nvl.isa</groupId>
      <artifactId>api-video-analytics</artifactId>
      <version>6.0.5.2</version>
      <scope>compile</scope>
    </dependency>

  </dependencies>

  <build>

    <sourceDirectory>./src/</sourceDirectory>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.4.1</version>
        <configuration>
          <archive>
            <manifest>
              <addClasspath>true</addClasspath>
              <classpathPrefix>lib/</classpathPrefix>
              <mainClass>owner.isa.hive.HiveReporter</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <descriptors>
            <descriptor>./bundle.xml</descriptor>
          </descriptors>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>3.3.1</version>
        <configuration>
          <encoding>${project.build.sourceEncoding}</encoding>
        </configuration>
      </plugin>
    </plugins>

  </build>

</project>
